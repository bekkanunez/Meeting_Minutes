{
  "name": "meeting-minutes",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "dependencies": {
    "axios": "^1.2.1",
    "bootstrap": "^5.2.3",
    "dotenv": "^16.0.3",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && npm install && react-scripts start",
    "build": "cd client && react-scripts build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
